# Technical Notes

- Use hexagonal architecture at all times. Keep business logic isolated from CLI, filesystem, network, storage, and other external adapters.
- Prefer functional testing before calling any feature complete. Unit tests are useful, but they do not prove the tool works the way the design intends.
- Take an agile approach to development. Avoid waterfall: underspecify when useful, prototype early, learn from the result, and refine from working behavior.
- `docs/docs/explanation/design.md` is the authoritative design; update it in the same PR when implementation forces an unrecorded decision. The phased implementation plan is `.journal/001/PLAN.md`.
- Release Please auth: repo variable `IMGOCI_RELEASE_APP_ID` + secret `IMGOCI_RELEASE_APP_PRIVATE_KEY`, sourced from 1Password item `imgoci-release-please` (Development vault, `op` CLI; app_id field + key.pem file). Confirm the app is installed on the repo before the first release.
- Repository settings are code: `.github/repository-settings.toml`, applied with `uv run .github/scripts/configure_github_repo.py apply --repo imgoci/go-oci-blob` (applied 2026-08-10; a few toggles are GitHub-UI-only, see `[unsupported]`).
- Do not infer support for less-common Distribution paths from mainstream clients. The 2026-08-12 live corpus found registry-specific gaps in empty blobs, chunked uploads, and safety rejection; refresh the dated matrix before turning any such behavior into a default or documentation claim.
- The provider-neutral compatibility harness is the separate consumer module under `test/`; its agent-operated lifecycle and cleanup procedure is `docs/docs/how-to/refresh-registry-compatibility.md`. Live credentialed campaigns stay out of CI. The nine-registry ledger in `.journal/005/REGISTRY_TEST_RESULTS.md` is historical evidence from the evolving disposable harness; rerun with the merged harness before publishing current claims.
- Amazon ECR Private `BLOB_MOUNTING` remains enabled in the shared account's `us-east-2` registry by explicit user request.
- E2E tests are build-tagged `e2e` (skip in plain `go test`; run via `go test -tags e2e` or the `root:test-e2e` moon task, which `root:check` includes so CI covers them). Put new e2e tests in e2e-tagged files.
- `moon run` breaks locally on machines with a legacy `~/.proto` store: moon v2 prepends `~/.proto/{tools/proto,shims,bin}` to task PATH, shadowing mise pins (go 1.26.5 vs pinned 1.26.4 → "compile: version mismatch"). CI is unaffected (no proto store on runners). Workaround: run the task's command directly via `mise exec --`. Real fix is a template-go-level decision (clean the proto store vs `.prototools` mirror pins vs `mise exec` wrappers in moon.yml).
- golangci-lint's shared cache replays stale findings for deleted worktree paths (generated-file filter fails open when the file is gone). Run `golangci-lint cache clean` after removing a worktree.
- Scripted-transport tests: `newTestContext` defaults to a zero RetryPolicy so conversations stay single-shot; retry behavior tests opt into a fast policy. Unexpected mockery calls in worker goroutines Goexit without filling result futures — script prospective concurrent requests with `.Maybe()` in failure-path tests.
- Registry credentials are origin-scoped: only marked same-origin requests use the caller's registry transport. Off-origin upload/storage Locations use `WithStorageTransport` (or the unauthenticated default), strip authorization, cookies, and Referer, and reject non-HTTP(S) redirects.
- Pull, resume, probe, fallback, and parallel-chunk requests share one lifetime `RetryPolicy.MaxAttempts` budget. Every 206 response must carry a valid `Content-Range` matching the requested interval before bytes are exposed.
- Upload request bodies use ownership-aware bounded staging because `http.RoundTripper` may consume or close `Request.Body` asynchronously after returning. Preserve prompt Close, first-progress publication for streaming producers, exact-size validation, transport-visible byte accounting, and replay synchronization. Active staging is capped at four 256 KiB buffers per request; the process-wide idle cache retains at most 2 MiB.
- Parallel Pull uses a fixed worker set and a client-local bounded chunk cache, retaining at most one chunk buffer per configured worker. Each scheduled chunk accepts at most 16 successful partial responses; deterministic redirect-policy failures are not retried.
- Performance benchmarks and public regressions live in `internal/perf`. Historical root-package results require `benchstat -ignore pkg` for comparison. On the Apple M4 Max reference run, Pull tracked capped throughput through roughly 2 GiB/s before a roughly 2.6 GiB/s CPU ceiling, while Push tracked through 4 GiB/s and reached 6.78 GiB/s against an 8 GiB/s cap.
- User docs are MkDocs Material under `docs/` (Diátaxis layout; CI gate is `uv run --locked mkdocs build --strict`). The API reference deliberately lives only in godoc/pkg.go.dev. Tutorial and how-to pages embed outputs and snippets from real verified runs — re-execute them when error strings, the public API, or pinned recipe dependencies change.
- The compatibility reference page renders per-registry tabs with icon results and per-feature hover tooltips, generated from the `.journal/005` ledger data. Material shrinks both `<table>` and its `.md-typeset__table` wrapper to content width; the full-width overrides in `docs/docs/stylesheets/palette.css` are scoped to `.tabbed-block` on purpose.
